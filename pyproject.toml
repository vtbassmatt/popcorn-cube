[project]
name = "popcorn-cube"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dj-database-url>=3.1.2",
    "django>=6.0.5",
    "django-ez-tasks>=0.1.0",
    "django-runserveronhostname>=0.3.0",
    "python-decouple>=3.8",
    "whitenoise[brotli]>=6.12.0",
]

[dependency-groups]
prod = [
    "gunicorn>=26.0.0",
]
