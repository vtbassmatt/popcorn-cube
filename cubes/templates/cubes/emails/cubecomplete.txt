::: POPCORN CUBE :::

:: {{ submission.cube.name }} ::

With {% firstof submission.player.get_short_name submission.player.get_username %}'s submission, this cube is complete!

Cards selected this round:
{% for selected_submission in round_submissions %}
- {% firstof selected_submission.player.get_short_name selected_submission.player.get_username %}: {{ selected_submission.card_name }}{% if selected_submission.related_to %} ({{ selected_submission.related_to }}){% endif %}
{% endfor %}
You can view it here: {{ url_base }}{% url 'cube-detail' submission.cube.pk %}
